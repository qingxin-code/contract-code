# 项目进度跟踪文档

## 阶段一：项目规划与结构设计 (✅ 已完成)
**时间**：2026-03-17
**内容**：
- 前后端技术栈与响应格式规范确立
- `schema.sql` 数据库及表结构设计完毕 (包含 `sys_user` 及 `contract` 依赖关系隔离)

## 阶段二：后端 API 系统开发 (✅ 已完成)
**时间**：2026-03-17
**内容**：
- Maven 多模块基础依赖构建、`application.yml` 集成
- 全局异常 `GlobalExceptionHandler` 与自定义异常拦截
- `jjwt-api` 最新语法版本编写的 `JwtUtil` 及 `LoginInterceptor` 拦截器配置
- 用户注册、登录鉴权控制器服务
- 合同增、删、改、查、分页以及数据权限过滤 RESTful 服务
- 修复了 `jjwt` 0.12.5 版本运行时缺失实现类的问题，并增强了密钥的 Base64 安全处理。

## 阶段三：前端 CMS 系统开发 (✅ 已完成)
**时间**：2026-03-17
**内容**：
- `Vite` + `Vue3(ts)` 脚手架创建，安装 TailwindCSS 和 Naive UI 组件
- 配置全局粉色主题及主入口应用骨架装载
- 基于 Axios 的网络请求携带及 401 路由守卫拦截
- 完成登录注册视图 (`Login.vue`)
- 完成核心功能视图 (`Layout.vue`, `Dashboard.vue`, `List.vue`, `Form.vue`)
- 完成组件和后端 API 的双向数据绑定
- 修复了 Tailwind CSS v4 环境下 PostCSS 插件配置错误，适配了新的 `@tailwindcss/postcss` 模块。

## 阶段六：系统缺陷修复与美学强化 (✅ 已完成)
**时间**：2026-03-17
**进展详情**：
- **致命缺陷修复**：修复了 `MerchantController` 调用 `LoginInterceptor` 时报“找不到符号 `getCurrentId`”的编译错误。
- **架构重构**：在 `LoginInterceptor` 中引入 `ThreadLocal` 模式，并重构全站 Controller 统一通过静态方法安全获取当前用户，移除了对 `HttpServletRequest` 的硬依赖。
- **启动异常修复 [新增]**：解决了由于 Maven 本地仓库文件下载不全导致的 `CorsConfig` 内省失败异常（`Failed to introspect Class`）。通过 `mvn dependency:resolve -U` 强制重新拉取了 Spring 核心库，目前后端已可成功启动并就绪。
- **全站粉色主题定制**：
  - 配置 Naive UI 全局 `NConfigProvider`，将 `primaryColor` 及其所有状态颜色（Hover/Pressed）锁定为粉色 (#ec4899)。
  - 统一 Tailwind 语义化类名，将所有模块（合同、客商、设置、布局）的硬编码色值替换为 `primary` 变量。
  - **视觉细节修复 [新增]**：重构仪表盘“快捷通道”图标配色，移除蓝/紫/橙杂色，全量适配粉色系视觉。
- **个人中心与退出逻辑优化 [已闭环]**：
  - **登录接口增强**：扩展了 `AuthController` 登录返回结构，确保后台 `nickname` 字段能够实时下发至前端。
  - **极致粉色美学适配**：在 `App.vue` 中深度覆盖了 Naive UI 的 `warning` 报警色系，将原本突兀的橙色按钮强制修正为品牌粉色 (#ec4899)，确保退出弹窗等交互细节的视觉纯净。
  - **交互体验补完 [全面闭环]**：
    - 修复了侧边栏菜单选中状态同步异常的问题，通过重构 `activeMenu` 逻辑并细化 `contract/create` 路径优先级，实现了对所有功能模块（含二级入口）的精确高亮感应。
    - 补全了“新建合同”菜单项的动态样式绑定，确保面包屑导航与侧栏状态完全一致。
  - **动态身份联动**：修复了登录后昵称显示“管理员”的问题，现在能够根据账号真实姓名动态渲染欢迎语及侧栏信息。
- **系统配置中心全面落库 [已完成]**：
  - **核心链路贯通**：实现了 `SysConfig` 模块的前后端联调，支持“系统运行名称”与“系统主题色”的持久化存取。
  - **全站动态同步**：引入 `app` 全局状态管理，确保设置页面保存后，侧向导航栏标题等全局组件能够即时响应配置变更，无需刷新页面。

## 阶段七：验证联调及项目收尾 (⏳ 待执行)
**目前状态**：
后端接口与前端视觉已完成深度打磨。
请在本地数据库中重新运行更新后的 `sql/schema.sql` 脚本，随后启动项目进行最终验收。
